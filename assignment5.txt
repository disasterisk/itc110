1) dictionary = {}
2) dictionary = { "foo" : 42 }
3) A dictionary's indexes are named, not numbered.
4) You will get a KeyError.
5) "cat" in spam and "cat in spam.keys() do the same thing.
6) "cat" in spam looks for keys named cat. "cat" in spam.values() looks at key values.
7) spam.setdefault("color","black")
8) pprint.pprint()
